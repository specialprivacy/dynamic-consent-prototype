<div class="layout-column">
  {{#paper-toolbar class="layout-row"}}
    {{#paper-toolbar-tools}}
      {{paper-button iconButton=true class="hidden"}}
      <h2>S8-C</h2>
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}

  <div class="layout-row">
    <div class="flex-gt-sm-25"/>
    {{#paper-card class="md-padding flex" as |card|}}
      {{#card.content}}
        <div class="layout-row md-padding">
          Thank your for choosing our free sightseeing recommendation service, which will provide you with customised locations to visit based on your personal interests.
        </div>

        <div class="layout-row md-padding">
          We are positive that you will love our recommendations. Starting on the next screen, we will walk you through three simple steps to get the most out of them.
        </div>

        <div class="layout-row">
          <div class="flex-40"/>
          {{#paper-button class="flex-20" raised=true primary=true onClick=(action "proceed")}}Get started{{/paper-button}}
          <div class="flex-40"/>
        </div>
      {{/card.content}}
    {{/paper-card}}
    <div class="flex-gt-sm-25"/>
  </div>
</div>